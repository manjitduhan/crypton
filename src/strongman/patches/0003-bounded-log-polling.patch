diff --git a/strongMan/apps/connections/views/LogHandler.py b/strongMan/apps/connections/views/LogHandler.py
--- a/strongMan/apps/connections/views/LogHandler.py
+++ b/strongMan/apps/connections/views/LogHandler.py
@@ -10,33 +10,36 @@
 class LogHandler(object):
+    MAX_WAIT_SECONDS = 10
     def __init__(self, request):
         self.newest_log = None
         self.id = int(request.POST.get('id'))
 
     def handle(self):
         response = dict(logs=[])
         self._delete_old_logs()
         if self.id < 0:
             logs = self._get_logs()
         else:
             logs = self._get_new_logs()
         for log in logs:
             log_dict = dict(id=log.id, message=log.message, name=log.connection.profile)
             log_dict['timestamp'] = log.timestamp.strftime("%Y-%m-%d %H:%M:%S")
             response['logs'].append(log_dict)
         return JsonResponse(response)
 
     def _delete_old_logs(self):
         time_threshold = timezone.now() - timedelta(minutes=5)
         LogMessage.objects.filter(timestamp__lt=time_threshold).delete()
 
     def _get_logs(self):
-        while LogMessage.objects.all().count() == 0:
+        deadline = time.monotonic() + self.MAX_WAIT_SECONDS
+        while LogMessage.objects.all().count() == 0 and time.monotonic() < deadline:
             time.sleep(1)
         return LogMessage.objects.all().order_by('timestamp')
 
     def _get_new_logs(self):
         logs = LogMessage.objects.filter(pk__gt=self.id).order_by('timestamp')
-        while logs.count() == 0:
+        deadline = time.monotonic() + self.MAX_WAIT_SECONDS
+        while logs.count() == 0 and time.monotonic() < deadline:
             time.sleep(1)
             logs = LogMessage.objects.filter(pk__gt=self.id).order_by('timestamp')
         return logs
